{
  "name": "gre-verbal",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "turbo start",
    "test": "turbo test",
    "lint": "turbo lint",
    "seed": "turbo run seed --filter=@gre/api"
  },
  "devDependencies": {
    "turbo": "^2",
    "typescript": "^5"
  }
}
